test_submodules_subm1_newer
===========================

Repository to test ansible's git module submodule handling

This repository needs to have one more file than test_submodules_subm1 has in
the top level directory
